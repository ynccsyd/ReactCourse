M/-array map kullanimi
// https://developer.mozilla.org/tr/docs/Web/JavaScript/Reference/Global_Objects/Array/map
const USERS = {"AYSE", "MehMet", "TugCE", "AkSEL"]
// userName icinde orjinal isim kalsin,
// // short Name icinde ilkharf buy uk . (A.)
// // new Name icinde ilkharf buy uk olsun
/ {/ {userName: "AYSE", shortName: "A.", new Name: "Ayse")
const NEW_USERS = USERS.map( user => user.tolowercase() )
console. log(NEW_USERS)
const USERS_083 = USERS.map( item =>
return (
userName: item, shortName: 's(item(0]).", new Name: 's(item[0].toUpperCase())s(item.slice(1).
toLowerCase())"
console. log(USERS_0B3)
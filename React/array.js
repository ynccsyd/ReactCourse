const users = [
{
name: "Mehmet",
age: 18,
},
{
name: "Murat",
age: 30,
},
{
name: "Sevilay",
},
]
/*
push
maр
find
filter
some
every
includes
*/
// push
users.push("Ayse");
users.push("Fatma");
console. log(users);
// mар
// users.map((item)
// console.log(item.name);

// find
// const result = users.find((item) => item.name === "Mehmet" && item.age > 20);
// console.log(result);
// filter
// const filtered = users filter((( name, age }) => name === "Mehmet" 66 age < 20);
// // console.log(filtered);
// some
// const some = users.some((item) => item.age === 11);
// console. log(some);
// every
// const every = users.every((item) => item.age > 20);
// console.log(every);
// includes
// const meyveler = ["elma", "armut", "muz");
// const isIncluded = meyveler.includes("portakal");
// console. log(isIncluded);